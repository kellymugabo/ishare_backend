name: ishare_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.5.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 🌍 Localization
  flutter_localizations: 
    sdk: flutter
  intl: 0.20.2 

  # 🗺️ MAPS (Switched to OpenStreetMap - Free!)
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  # google_maps_flutter: ^2.5.3  <-- REMOVED

  url_launcher: ^6.2.1
  cupertino_icons: ^1.0.8
  dio: ^5.3.3 
  flutter_riverpod: ^2.4.9 
  flutter_secure_storage: ^9.0.0 
  image_picker: ^1.0.4 
  firebase_core: ^4.2.1
  firebase_messaging: ^16.0.4
  http: ^1.1.0
  path: ^1.8.3
  flutter_image_compress: ^2.1.0
  file_picker: ^10.0.0
  provider: ^6.0.5
  qr_flutter: ^4.1.0
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  # ⚡ Enable code generation for translations
  generate: true 

  assets:
    - assets/images/